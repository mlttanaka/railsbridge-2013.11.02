<% if @post.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
			<p class="text-danger">
	    	<% @post.errors.full_messages.each do |msg| %>
	     		<%= msg %>.<br />
	    	<% end %>
    	</p>
		</div>
  </div>
<% end %>